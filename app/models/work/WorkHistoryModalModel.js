define([
  'underscore',
  'backbone'
], function(_, Backbone) {

	defaults: {
		"id": "001",
		"parent_id": "0"
	    "username": "username", 
	    "item": "changed smth"
	}
  
  var WorkHistoryModalModel = Backbone.Model.extend({});

  return WorkHistoryModalModel;

});